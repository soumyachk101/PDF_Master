{
    "name": "pdfkit-monorepo-root",
    "private": true,
    "scripts": {
        "start": "npm start --prefix backend",
        "build": "npm run build --prefix backend --if-present",
        "postinstall": "npm install --prefix backend"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}